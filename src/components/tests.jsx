import pic from '../assets/pics/inner-circle.png'
export const Tests = () => {

    return <section className="days-wrapper">
        <div className="days">
            <p><img src={pic} alt={pic} /></p>
        </div>
    </section>
}